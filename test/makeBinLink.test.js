import test from 'ava'
import sinon from 'sinon'
import rewire from 'rewire'
let utils = rewire('../src/utils')

test.todo('get flyway version directory')
test.todo('bin link exists')
test.todo('flyway version directory does not exist')
